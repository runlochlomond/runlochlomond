---
---

---
label: Races
hide_body: true
fields:
- type: field_group_list
  name: Races
  label: Races
  fields:
  - type: text
    name: title
    label: Name of race
    config:
      required: true
  - type: datetime
    name: race_date
    label: Race date
    config:
      required: true
  - type: datetime
    name: entries_closing_date
    label: Entry closing date
    config:
      required: false
  - type: textarea
    name: description
    label: Description
    config:
      required: true
  - type: field_group
    name: price
    label: Price
    fields:
    - type: text
      name: sa
      label: Price for Scottish Atheltics affliated runners
      config:
        required: false
    - type: text
      name: non-sa
      label: Price for non-Scottish Atheltics affliated runners
      config:
        required: true
  - type: text
    name: route
    label: Route
    description: Enter a link to the route.
  - type: field_group
    name: entry
    label: How to enter
    fields:
    - type: text
      name: link
      label: Link
      description: Link to entrycentral/stuweb where runners can enter.
      config:
        required: true
    - type: text
      name: text
      label: What should the link say?
      config:
        required: true
  - type: file
    name: image_link
    label: Image
    description: Upload a image for the race.
  config:
    labelField: title
  description: List the races here.
pages:
- _data/races.yml
